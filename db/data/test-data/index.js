// require in and export out all test data
const userData = require('../test-data/users');
const articleData = require('../test-data/articles');
const commentData = require('../test-data/comments');
const topicData = require('../test-data/topics');

//data got deleted when stashed gr9

module.exports = {
    userData,
    articleData,
    commentData,
    topicData
}